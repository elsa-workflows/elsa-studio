# Elsa Studio Diagnostics OpenTelemetry

Adds the Studio-side OpenTelemetry diagnostics feature shell.

The first implementation slice establishes feature gating, API client contracts, normalized DTOs that mirror Core read contracts, service registration, and the diagnostics menu entry. UI investigation tabs, live SignalR observation, URL-state mapping, and cross-links are implemented by the follow-up user-story tasks.
