@inherits StudioComponentBase

<MudForm>
    <MudStack Spacing="5">
        @foreach (ActivityInputDisplayModel inputModel in InputDisplayModels)
        {
            if (IsVisibleInput(inputModel))
            {
                @inputModel.Editor
            }
        }
    </MudStack>
</MudForm>