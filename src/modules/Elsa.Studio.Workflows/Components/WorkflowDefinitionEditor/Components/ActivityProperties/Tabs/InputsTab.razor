@inherits StudioComponentBase

<MudForm>
    <MudStack Spacing="5">
        @foreach (var inputModel in InputDisplayModels)
        {
            <div @key="@inputModel.Index">
                @inputModel.Editor
            </div>
        }
    </MudStack>
</MudForm>