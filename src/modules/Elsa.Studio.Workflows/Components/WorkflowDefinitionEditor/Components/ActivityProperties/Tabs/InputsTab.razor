@inherits StudioComponentBase

<div class="pa-2">
    <MudForm>
        <MudStack Spacing="3">
            @foreach (var inputModel in InputDisplayModels)
            {
                @inputModel.Editor
            }
        </MudStack>
    </MudForm>
</div>